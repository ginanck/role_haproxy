# Disable privilege dropping for container compatibility
global(
  processInternalMessages="on"
  umask="0000"
)

module(load="imudp")
input(type="imudp" port="514")

if $programname == 'haproxy' then {{ haproxy_log_file }}
if $programname == 'haproxy' then stop
